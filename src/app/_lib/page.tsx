export default function PrivateFolder(){
    return (
        <>
        <div>It is not create the route.when we trying to access the route we get page not found</div>
        <div>We can also use %5 instead of _(underscore) to create the private route.</div>
        </>
    )
}